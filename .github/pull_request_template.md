<!-- Every section below is expected. Delete none of them; write "none" where a
     section does not apply. -->

## What this changes

<!-- What the pull request does, and why. -->

## ADR check

<!-- MANDATORY — the check is required on every pull request, the ADR is not
     (ADR-0001). State one of the three outcomes:

     - **None** — this pull request embarks no lasting decision. Most do not.
     - **Create** — it embarks one. Name the ADRs added, drafted `Proposed`, and
       listed in the index.
     - **Supersede** — it replaces a recorded decision. Name the ADR superseded
       and the successor drafted for it. Never flip a status yourself.
     - ⚠️ **Conflicts with ADR-NNNN (<title>)** — it contradicts an accepted
       decision. Say precisely how, and leave the call to the maintainer.

     The test for "lasting": if the implementation changed but the decision
     stood, would the ADR need editing? -->

**Outcome:**

## Catalog

<!-- Only if the catalog changed; otherwise "none".

     - Which patterns or roles were added, changed or removed.
     - Confirm `python3 catalog/generate.py` was run and the diff reviewed.
     - Note any entry whose relation to another pattern was decided here — a
       declension, a specialisation, or two homonyms deliberately kept apart —
       and the assertions that settled it (ADR-0007). -->

## How this was verified

<!-- Beyond CI: what you ran, and what it showed. -->
