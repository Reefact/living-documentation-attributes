<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <!-- Infrastructure references the domain, never the reverse. The direction is the pattern. -->
  <ItemGroup>
    <ProjectReference Include="..\DesignPatternCatalog.DomainDrivenDesign\DesignPatternCatalog.DomainDrivenDesign.csproj" />
    <ProjectReference Include="..\DesignPatternCatalog.Usage.BloodBank.Domain\DesignPatternCatalog.Usage.BloodBank.Domain.csproj" />
  </ItemGroup>

</Project>
