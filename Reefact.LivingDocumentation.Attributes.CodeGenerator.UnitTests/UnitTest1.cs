namespace Reefact.LivingDocumentation.Attributes.CustomTool.UnitTests {

    //[UseReporter(typeof(BeyondCompareReporter))]
    //public class UnitTest1 {

    //    [Fact]
    //    public void Test1() {
    //        DesignPatternFileGenerator generator        = new();
    //        IVsGeneratorProgress       generateProgress = new GeneratorProgress();
    //        string                     inputFilePath    = "C:\\Users\\sylva\\source\\repos\\Reefact.LivingDocumentation.Attributes\\Reefact.LivingDocumentation.Attributes.CustomTool.UnitTests\\Resources\\visitor.design-pattern";
    //        string                     inputContent     = File.ReadAllText(inputFilePath);

    //        generator.Generate(inputFilePath, inputContent, "Cacib.Grichka.Shared", out IntPtr outputContentPtr, out int outputLength, generateProgress);

    //        try {
    //            byte[] bytes = new byte[outputLength];
    //            Marshal.Copy(outputContentPtr, bytes, 0, outputLength);
    //            string result = Encoding.UTF8.GetString(bytes);
    //            Approvals.Verify(result);
    //        } finally {
    //            Marshal.FreeCoTaskMem(outputContentPtr);
    //        }
    //    }

    //}

}