{
  "catalog": "Posa2",
  "name": "DoubleCheckedLockingOptimization",
  "summary": "Reduces contention where a critical section must run exactly once, by testing a flag before taking the lock and testing it again after taking it.",
  "reference": {
    "author": "Douglas Schmidt, Michael Stal, Hans Rohnert, Frank Buschmann",
    "work": "Pattern-Oriented Software Architecture, Volume 2",
    "year": 2000
  },
  "inherited": false,
  "roles": [
    {
      "name": "JustOnceCriticalSection",
      "targets": [
        "Method"
      ],
      "repeatable": false,
      "links": [],
      "summary": "The code that must execute exactly once, an initialisation typically. It is reached rarely compared with the accesses that find the work already done, which is the entire reason for paying for two tests instead of one lock."
    },
    {
      "name": "Mutex",
      "targets": [
        "Field",
        "Property"
      ],
      "repeatable": true,
      "links": [],
      "summary": "The lock that serializes the threads which find the flag unset. It is contended on the first pass and untouched afterwards, so a measurement taken once the flag is set will show this lock costing nothing and prove nothing."
    },
    {
      "name": "Flag",
      "targets": [
        "Field",
        "Property"
      ],
      "repeatable": false,
      "links": [],
      "summary": "Says whether the critical section has already run, and is read twice: once before the lock as the optimisation, and once inside it as the correctness. Unless it is written and read atomically, the thread that skips the lock can reach a value that is published but not yet built \u2014 which is why this pattern is famous for being wrong in a way testing does not find."
    }
  ]
}
